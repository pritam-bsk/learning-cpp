class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int n = prices.size();
        if(n==1) return 0;
        int min = prices[0]
        for(int i{};i<n;i++){
            min = min(min,prices[i]);
        }

    }
};